/*
 * ir.h - SSA intermediate representation for the free C compiler.
 * Redirects to the canonical header in include/ir.h.
 * This file exists so that #include "ir.h" from src/cc/ finds
 * the SSA IR definitions via the local path.
 */
#ifndef FREE_SSA_IR_H
#include "../../include/ir.h"
#endif
